// radius/* tokens from Figma variables
export const radius = {
  card: 20, // radius/card (small cards, inputs)
  cardLarge: 32, // radius/card used on larger photo cards
  chip: 999, // radius/chip
  pill: 100, // radius/pill (search bars, pill buttons)
  outer: 28, // radius/outer (device frame / big CTA buttons)
} as const;
